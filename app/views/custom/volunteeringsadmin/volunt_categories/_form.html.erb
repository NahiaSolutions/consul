<%= form_for [:volunteeringsadmin, @volunt_category]  do |f| %>
  <%= render 'shared/errors', resource: @volunt_category %>

  <%= f.label :name, "Nombre de la categoria" %>
  <%= f.text_field :name, label: false %>

  <%= f.submit "Guardar" %>
<% end %>