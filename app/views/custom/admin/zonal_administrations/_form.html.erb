<%= form_for [:admin, @zonalAdministration]  do |f| %>
  <%= render 'shared/errors', resource: @zonalAdministration %>

  <%= f.label :name, "Nombre de la administración" %>
  <%= f.text_field :name, label: false %>

  <%= f.submit "Guardar" %>
<% end %>