<% content_for :body_class, "home-page" %>

<% content_for :canonical do %>
  <%= render "shared/canonical", href: root_url %>
<% end %>

<% provide :social_media_meta_tags do %>
  <%= render "shared/social_media_meta_tags",
              social_url: root_url %>
<% end %>
<!--Banner de consul-->
<%= render "header", header: @header %>
<!---->

<main>
    <div class="row">
      <div class="column">
        <h3 style="text-align:center;"><span style="border-bottom: 5px solid #72b951;"><%= t("welcome.cards.title")%></span></h3>
      </div> 
    </div>
    <br>
    <%= render "cards" %>
</main>